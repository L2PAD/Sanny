# 🔐 Актуальные данные для входа в систему

## Дата обновления: 3 декабря 2024

---

## 👨‍💼 АДМИНИСТРАТОРЫ

### Вариант 1 (Рекомендуется):
```
Email: admin@bazaar.com
Пароль: admin123
```

### Вариант 2:
```
Email: admin@marketplace.com
Пароль: admin123
```

**Функции админа:**
- Доступ к админ-панели `/admin`
- Управление товарами
- Управление категориями
- Просмотр аналитики
- Обработка выплат
- Просмотр детальной статистики заказов

---

## 👥 ТЕСТОВЫЕ ПОКУПАТЕЛИ

### Покупатель 1 (Рабочий):
```
Email: testcustomer@example.com
Пароль: customer123
Имя: Test Customer
```

### Покупатель 2:
```
Email: customer1@test.com
Пароль: password123
Имя: Анна Петренко
```

### Покупатель 3:
```
Email: customer2@test.com
Пароль: password123
Имя: Дмитро Коваленко
```

**Функции покупателя:**
- Просмотр товаров
- Добавление в корзину
- Оформление заказов
- Просмотр истории заказов
- Избранное
- Сравнение товаров

---

## 🔄 Как сбросить пароль вручную

Если нужно сбросить пароль для любого пользователя:

```bash
python3 << 'EOF'
import asyncio
from motor.motor_asyncio import AsyncIOMotorClient
from passlib.context import CryptContext

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

async def reset_password(email, new_password):
    client = AsyncIOMotorClient("mongodb://localhost:27017")
    db = client.marketplace_db
    
    hashed = pwd_context.hash(new_password)
    result = await db.users.update_one(
        {"email": email},
        {"$set": {"hashed_password": hashed}}
    )
    
    print(f"✅ Пароль для {email} обновлен!")
    client.close()

# Использование:
asyncio.run(reset_password("admin@bazaar.com", "новый_пароль"))
EOF
```

---

## 📧 Email уведомления

Для настройки email уведомлений о заказах, добавьте в `/app/backend/.env`:

```bash
# Email Configuration
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ваш-email@gmail.com
SMTP_PASSWORD=ваш-пароль-приложения
FROM_EMAIL=ваш-email@gmail.com
ADMIN_EMAIL=admin@bazaar.com
```

**Для Gmail:**
1. Включите двухфакторную аутентификацию
2. Создайте пароль приложения: https://myaccount.google.com/apppasswords
3. Используйте этот пароль в `SMTP_PASSWORD`

---

## 🧪 Тестирование авторизации

Проверить, работает ли авторизация:

```bash
curl -X POST "http://localhost:8001/api/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@bazaar.com","password":"admin123"}'
```

Должен вернуть:
```json
{
  "access_token": "eyJ...",
  "token_type": "bearer",
  "user": {
    "email": "admin@bazaar.com",
    "full_name": "Admin User",
    "role": "admin"
  }
}
```

---

## 🔒 Безопасность

**В продакшене обязательно:**
1. ❗ Смените все пароли
2. ❗ Обновите `JWT_SECRET_KEY` в `.env`
3. ❗ Используйте сложные пароли (минимум 12 символов)
4. ❗ Настройте HTTPS
5. ❗ Ограничьте попытки входа

---

## 📞 Поддержка

Если возникли проблемы со входом:
1. Проверьте, что backend запущен: `sudo supervisorctl status backend`
2. Проверьте логи: `tail -f /var/log/supervisor/backend.err.log`
3. Убедитесь, что MongoDB работает: `sudo systemctl status mongodb`
4. Попробуйте сбросить пароль (см. раздел выше)

---

**Последнее обновление:** 3 декабря 2024, 14:50
**Статус:** ✅ Все учетные записи проверены и работают
